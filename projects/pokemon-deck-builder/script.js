const $=id=>document.getElementById(id),grid=$('grid'),teamEl=$('team'),statusEl=$('status');let data=[],team=JSON.parse(localStorage.getItem('pokemon-team-v2')||'[]');const pretty=s=>s[0].toUpperCase()+s.slice(1);async function load(){try{const list=await fetch('https://pokeapi.co/api/v2/pokemon?limit=60').then(r=>r.json());const details=await Promise.all(list.results.map(async p=>{const d=await fetch(p.url).then(r=>r.json());return{id:d.id,name:d.name,image:d.sprites.other['official-artwork'].front_default||d.sprites.front_default,types:d.types.map(x=>x.type.name)}}));data=details;statusEl.textContent='60 Pokémon loaded. Search and build your team.';render()}catch(e){statusEl.textContent='Could not reach PokéAPI. Check your internet connection.'}}function renderTeam(){teamEl.innerHTML=Array.from({length:6},(_,i)=>{const p=team[i];return p?`<div class="slot filled"><img src="${p.image}" alt="${p.name}"><b>${p.name}</b></div>`:'<div class="slot">Empty slot</div>'}).join('');$('count').textContent=team.length;localStorage.setItem('pokemon-team-v2',JSON.stringify(team))}function render(){const q=$('search').value.trim().toLowerCase(),t=$('type').value,s=$('sort').value;let list=data.filter(p=>(!q||p.name.includes(q)||String(p.id)===q)&&(!t||p.types.includes(t)));list.sort((a,b)=>s==='name'?a.name.localeCompare(b.name):a.id-b.id);grid.innerHTML=list.map(p=>{const inTeam=team.some(x=>x.id===p.id);return`<article class="card"><div class="id">#${String(p.id).padStart(3,'0')}</div><img loading="lazy" src="${p.image}" alt="${p.name}"><h3>${p.name}</h3><div class="types">${p.types.map(x=>`<span>${x}</span>`).join('')}</div><button data-id="${p.id}" class="${inTeam?'remove':''}">${inTeam?'Remove from team':'Add to team'}</button></article>`}).join('')||'<p>No matching Pokémon.</p>';renderTeam()}grid.onclick=e=>{if(!e.target.matches('button[data-id]'))return;const id=Number(e.target.dataset.id),p=data.find(x=>x.id===id),i=team.findIndex(x=>x.id===id);if(i>=0)team.splice(i,1);else if(team.length<6)team.push(p);else{statusEl.textContent='Your team is full. Remove one first.';return}render()};['search','type','sort'].forEach(id=>$(id).addEventListener(id==='search'?'input':'change',render));renderTeam();load();

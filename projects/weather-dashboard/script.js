const codeText=c=>({0:'Clear',1:'Mainly clear',2:'Partly cloudy',3:'Overcast',45:'Fog',48:'Fog',51:'Drizzle',53:'Drizzle',55:'Drizzle',61:'Rain',63:'Rain',65:'Heavy rain',71:'Snow',80:'Showers',95:'Thunderstorm'}[c]||'Mixed conditions');
async function load(city){const status=document.getElementById('status');status.textContent='Loading weather…';try{const g=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(city)}&count=1&language=en&format=json`).then(r=>r.json());if(!g.results?.length)throw new Error('City not found');const p=g.results[0];const w=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${p.latitude}&longitude=${p.longitude}&current=temperature_2m,relative_humidity_2m,apparent_temperature,wind_speed_10m,weather_code&daily=weather_code,temperature_2m_max,temperature_2m_min&timezone=auto`).then(r=>r.json());document.getElementById('current').innerHTML=`<div><div class="meta">${p.name}${p.admin1?', '+p.admin1:''}, ${p.country}</div><div class="temp">${Math.round(w.current.temperature_2m)}°C</div><h2>${codeText(w.current.weather_code)}</h2></div><div class="stats"><div class="stat">Feels like<br><b>${Math.round(w.current.apparent_temperature)}°C</b></div><div class="stat">Humidity<br><b>${w.current.relative_humidity_2m}%</b></div><div class="stat">Wind<br><b>${Math.round(w.current.wind_speed_10m)} km/h</b></div><div class="stat">Updated<br><b>${new Date(w.current.time).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}</b></div></div>`;document.getElementById('forecast').innerHTML=w.daily.time.slice(0,5).map((d,i)=>`<article class="day"><span>${new Date(d+'T00:00').toLocaleDateString([], {weekday:'short'})}</span><strong>${Math.round(w.daily.temperature_2m_max[i])}°</strong><span>${codeText(w.daily.weather_code[i])}<br>Low ${Math.round(w.daily.temperature_2m_min[i])}°</span></article>`).join('');status.textContent='';}catch(e){status.textContent=e.message}}
document.getElementById('search').onsubmit=e=>{e.preventDefault();load(document.getElementById('city').value.trim())};load('Johannesburg');
